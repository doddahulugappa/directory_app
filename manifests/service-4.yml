apiVersion: v1
kind: Service
metadata:
    name: "djangoappcluster-26fb"
    labels:
        app: "djangoappcluster-26fb"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "djangoappcluster-26fb"